# workflow_call stub — delegates to hub canonical workflow.
# DO NOT edit logic here. Update hub source: ajrmooreuk/Azlan-EA-AAA/.github/workflows/validate-labels.yml
# Generated by S61.29.3

on:
  issues:
    types: [opened, edited, labeled, unlabeled]

permissions:
  issues: read
  pull-requests: read

jobs:
  check-labels:
    uses: ajrmooreuk/Azlan-EA-AAA/.github/workflows/validate-labels.yml@main
    secrets: inherit
