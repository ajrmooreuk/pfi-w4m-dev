# workflow_call stub — delegates to hub canonical workflow.
# DO NOT edit logic here. Update hub source: ajrmooreuk/Azlan-EA-AAA/.github/workflows/enforce-registry-link.yml
# Generated by S61.29.3

on:
  pull_request:
    types: [opened, edited, synchronize]

permissions:
  issues: read
  pull-requests: read

jobs:
  check-registry-link:
    uses: ajrmooreuk/Azlan-EA-AAA/.github/workflows/enforce-registry-link.yml@main
    secrets: inherit
